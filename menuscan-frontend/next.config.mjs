/** @type {import('next').NextConfig} */
const nextConfig = {
    devIndicators: {

        // Disable all development indicators
 
        buildActivity: false
 
      }
};

export default nextConfig;
